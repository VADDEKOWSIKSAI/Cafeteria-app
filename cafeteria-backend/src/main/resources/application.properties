# Server Configuration
server.port=8080

# Database Configuration (Update username/password as needed)
# Database Configuration (Update username/password as needed)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/cafeteria_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:vksai}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Kowsik15062004}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# JWT Secret (In production, use a strong environment variable)
app.jwt.secret=${APP_JWT_SECRET:9a4f2c8d3b7a1e6f45c8a0b3f2e1d5g9h8i7j6k5l4m3n2o1p}
app.jwt.expiration-ms=86400000

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:vaddekowsiksai5@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:yjfo bphj chyo texw}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

