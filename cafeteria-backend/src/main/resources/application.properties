server.port=${PORT:8080}

# Database Configuration (Local / Railway)
# Uses MYSQL_URL provided by Railway to auto-configure connection
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/cafeteria_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:Kowsik15062004}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# Prevent Railway crashes (HikariCP optimization)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000

# JWT Secret (In production, use a strong environment variable)
app.jwt.secret=${APP_JWT_SECRET:9a4f2c8d3b7a1e6f45c8a0b3f2e1d5g9h8i7j6k5l4m3n2o1p}
app.jwt.expiration-ms=86400000

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vaddekowsiksai5@gmail.com
spring.mail.password=yjfo bphj chyo texw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

